buildscript {
    repositories {
        mavenCentral()

        // for custom
//        flatDir { dirs 'libs' }
    }
    dependencies {
        // custom
//        classpath 'io.github.robwin:jgitflow-gradle-plugin:0.7.0-SNAPSHOT-CUSTOM'
//        classpath 'external.atlassian.jgitflow:jgit-flow-core:1.0-m5.1-CUSTOM'
//        classpath 'com.github.zafarkhaja:java-semver:0.9.0'
//        classpath 'org.eclipse.jgit:org.eclipse.jgit:3.7.1.201504261725-r'
//        classpath 'com.google.guava:guava:14.0.1'

        // regular
                classpath 'io.github.robwin:jgitflow-gradle-plugin:0.6.0'

    }
}

plugins {
    id 'java'
    id 'org.springframework.boot' version '3.1.2'
    id 'io.spring.dependency-management' version '1.1.2'
}

apply plugin: 'io.github.robwin.jgitflow'

group = 'com.example'
//version = '0.0.2-SNAPSHOT'

java {
    sourceCompatibility = '17'
}

repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
//    implementation 'external.atlassian.jgitflow:jgit-flow-core:1.0-m5.1'

}

tasks.named('test') {
    useJUnitPlatform()
}


task setGitCredentials{
    doLast{
        def user = System.getProperty('GITFLOW_USERNAME')
        def pwd = System.getProperty('GITFLOW_PASSWORD')
        if (user){
            project.setProperty('gitUsername', name)
        }
        if (pwd){
            project.setProperty('gitPassword', pwd)
        }
    }
}

initJGitflow{
    master = "master"
    develop = "develop"
    feature = "features/"
    release = "releases/"
    hotfix = "hotfix/"
    versiontag = 'v'
}


// changes just for commit
task getHomeDir {
    doLast {
        println gradle.gradleHomeDir
    }
}

task checkVersionProp{
    doLast {
        println project.hasProperty('version')
        println project.property('version')
    }
}

task checkGitCreds{
    doLast {
        def hasUser = project.hasProperty('gitUsername')
        println hasUser
        if (hasUser){
            println project.property('gitUsername')
        }
        def hasPwd = project.hasProperty('gitPassword')
        println hasPwd
        if (hasPwd){
            println project.property('gitPassword')
        }
    }
}
